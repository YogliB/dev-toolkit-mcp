[install]
exact = true
frozen = false
trust = true

[test]
coverage = true
coverageReporter = ["text", "lcov"]
coverageDir = "coverage"
coverageSkipTestFiles = true
coverageExclude = [
  "node_modules/**",
  "dist/**",
  "scripts/**",
  "**/*.spec.ts",
  "**/*.test.ts",
  "tests/**"
]
concurrent = true
maxConcurrency = 20
