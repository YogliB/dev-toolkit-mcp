{
	// Extends root with Node.js/Bun-specific options
	// Core package runs in Bun runtime with Node.js APIs
	"extends": "../../tsconfig.json",
	"compilerOptions": {
		// Node.js/Bun runtime libraries
		"lib": ["ESNext"],
		"target": "ESNext",
		"module": "Preserve",

		// Bun runtime types
		"types": ["bun"],

		// Package-specific path aliases
		"baseUrl": ".",
		"paths": {
			"@/*": ["src/*"]
		}
	},

	// Core package source files
	"include": ["src"],

	// Core package build artifacts
	"exclude": ["node_modules", "dist"]
}
